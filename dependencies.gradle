ext {
    // Kotlin
    kotlinVersion = '1.5.31'
    ktorVersion = '1.6.4'

    // Coroutines
    coroutinesVersion = "1.5.2"

    // Serializer
    kotlinSerializerVersion = "1.3.0"

    // Lint
    spotlessVersion = "5.12.4"

    // Kodein (DI)
    kodeinDiVersion = "7.5.0"

    // DB
    postgresVersion = "42.2.18"
    hikariVersion = "4.0.3"

    // SQLDelight
    sqlDelightVersion = '1.5.1'

    // Logging Utility
    logbackVersion = '1.2.1'

    // Liquibase Migration
    liquibaseVersion = '4.0.0'
}
