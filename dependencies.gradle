ext {
    // Kotlin
    kotlinVersion = '1.4.32'
    ktorVersion = '1.4.2'

    // Coroutines
    coroutinesVersion = "1.4.3"

    // Serializer
    kotlinSerializerVersion = "1.1.0"

    // Lint
    spotlessVersion = "5.12.4"

    // Kodein (DI)
    kodeinDiVersion = "7.5.0"

    // DB
    postgresVersion = "42.2.18"

    // SQLDelight
    sqlDelightVersion = '1.5.0'

    // Logging Utility
    logbackVersion = '1.2.1'

    // Liquibase Migration
    liquibaseVersion = '4.0.0'
}