ext {
    // Kotlin
    kotlinVersion = '1.5.0'
    ktorVersion = '1.5.4'

    // Coroutines
    coroutinesVersion = "1.4.3"

    // Serializer
    kotlinSerializerVersion = "1.2.0"

    // Lint
    spotlessVersion = "5.17.0"

    // Kodein (DI)
    kodeinDiVersion = "7.5.0"

    // DB
    postgresVersion = "42.2.18"
    hikariVersion = "4.0.3"

    // SQLDelight
    sqlDelightVersion = '1.5.0'

    // Logging Utility
    logbackVersion = '1.2.1'

    // Liquibase Migration
    liquibaseVersion = '4.0.0'
}